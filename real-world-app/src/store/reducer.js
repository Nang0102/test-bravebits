const initState = {
  todos: [],
  todoInput: "",
};
function reducer(state, action) {
  switch (
    action.type
    // ......... xử lí ở đây
  ) {
  }
}

export { initState };
export default reducer;
